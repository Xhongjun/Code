package com.xgdfin.io.bio;import java.io.IOException;import java.io.InputStream;import java.io.OutputStream;import java.net.ServerSocket;import java.net.Socket;/* * Copyright (C) 2017  ShenZhen xinLianZhengXin Co.,Ltd All Rights Reserved. * 未经本公司正式书面同意，其他任何个人、团体不得使用、复制、修改或发布本软件. * 版权所有深圳市信联征信有限公司 http://www.credlink.com/ */public class BIOServer {    public static void main(String[] args) {        InputStream inputStream = null;        OutputStream outputStream = null;        // 客户端请求数据        byte[] clientBuffer = new byte[512];        try {            ServerSocket serverSocket = new ServerSocket(8081);            System.out.println("bio 服务启动，开始监听端口：" + serverSocket.getLocalPort());            while (true) {                // 服务器开始监听请求（阻塞）                Socket socket = serverSocket.accept();                // 获得客户端请求信息                inputStream = socket.getInputStream();                inputStream.read(clientBuffer);                System.out.println("bio 服务端获得客户端请求数据：" + new String(clientBuffer));                Thread.sleep(4000);                System.out.println("bio 服务端正在处理数据，请稍等...");                // 服务端回写数据                outputStream = socket.getOutputStream();                outputStream.write("bio server 业务处理完毕，请知悉".getBytes());                outputStream.flush();            }        } catch (Exception e) {            e.printStackTrace();        } finally {            try {                if (inputStream != null) {                    inputStream.close();                }            } catch (IOException e) {                e.printStackTrace();            }            try {                if (outputStream != null) {                    outputStream.close();                }            } catch (IOException e) {                e.printStackTrace();            }        }    }}